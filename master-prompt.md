first up think hard and use agents (look in the agents folder, they might help you)!

here are your tasks:

* the workbench needs to be responsive fully, the header is not responsive
* I need a way to register a user via the server e.g. database inserts or endpoint which is just reachable from the deployed server, present me with options how we can solve this

repeat all that in your own words, and wait for me to confirm before starting. any questions? 
